# 发布签名配置模板。
#
# 复制成 android/key.properties 后填入自己的值 —— 该文件已在 .gitignore 中排除,
# 绝不入库。密钥库(.jks)与口令请自行离线备份:丢了就无法给老用户推覆盖升级,
# 只能让他们卸载重装,App 内数据全清。
#
# 缺本文件时 release 会回落到 debug 签名(见 android/app/build.gradle.kts),
# 本机 `flutter run --release` 照常可用,但对外分发必须用正式签名。

# 密钥库路径。相对路径以 android/ 为基准(rootProject.file 解析)。
storeFile=upload-keystore.jks
storePassword=你的密钥库口令
keyAlias=upload
keyPassword=你的密钥口令
